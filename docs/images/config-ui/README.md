# Configuration UI screenshots

Screenshots for the [Configuration UI README](../../scubagoggles/ui/README.md).

| Filename | Description |
|----------|-------------|
| `main-tab.png` | Main tab — organization fields and baseline/product selection |
| `omit-policies-tab.png` | Omit Policies tab with a configured omission |
| `preview-export.png` | Preview tab showing generated YAML |

When updating screenshots, use a consistent window size and theme (light or dark) so the documentation looks cohesive.
